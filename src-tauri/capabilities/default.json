{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "LogRadar default capability: grants the main window access to the Tauri core IPC layer (for the app's custom open_file/get_lines/search/export/workspace_* commands), the dialog plugin (file open dialog), and window title control.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "core:window:allow-set-title"
  ]
}
